/**
 * @fileoverview A collection of utility functions.
 * @requires validators
 * @requires calculates
 * @requires strings
 * @requires objects
 * @requires arrays
 * @requires htmls
 * @module utils
 * */

import { Calculates } from '@/utils/calculates';
import { Validators } from '@/utils/validators';
import { Relations } from '@/utils/relations';
import { Strings } from '@/utils/strings';
import { Objects } from '@/utils/objects';
import { Arrays } from '@/utils/arrays';
import { Htmls } from '@/utils/htmls';

export const Utils = {
  Calculates,
  Validators,
  Relations,
  Strings,
  Objects,
  Arrays,
  Htmls,
};
